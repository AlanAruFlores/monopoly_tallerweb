document.addEventListener("DOMContentLoaded", function() {
    // Verificar si la URL contiene "volver-login"
    if (window.location.href.includes("volver-login")) {
        // Mostrar un popup
        alert("Por favor, inicia sesión para continuar.");
        // Otra opción es usar un modal o cualquier otro tipo de popup que prefieras
    }
});